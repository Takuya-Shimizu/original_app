<h1><%= @user.name %>のページ</h1>

<p>名前: <%= @user.name %></p>
<p>メールアドレス: <%= @user.email %></p>
<p>お気に入りした投稿:
  <% @favorites.each do |post| %>
    <%= link_to post.title, post_path(post.id) %>
  <% end %></p>
  
<%= link_to '新規に投稿する', new_post_path %>
<%= link_to "投稿一覧画面へ", posts_path %>