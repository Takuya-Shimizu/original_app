<h1><%= current_user.name %>のお気に入り一覧</h1>

<table>
  <thead>
    <tr>
      <th>お気に入り投稿</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @favorites.each do |favorite| %>
      <tr>
      <td><%= favorite.post.body %></td>
      <td><%= link_to '投稿詳細', favorite.post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '戻る', posts_path %>
<%= link_to '新規投稿', new_post_path %>